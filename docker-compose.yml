services:
  api:
    build: ./api
    ports:
      - "8080:8080"
    environment:
      - ALLOWED_ORIGIN=http://localhost:5173

  web:
    build: ./web
    ports:
      - "5173:80"
    depends_on:
      - api
